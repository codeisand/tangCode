import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'codeisand.tangent.app',
  appName: 'tangent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
